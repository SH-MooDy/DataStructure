#include <iostream>
using namespace std;

const int MAX = 1000;

int floor(int arr[], int start_index, int K) { if () }

int ceiling(int arr[], int end_index, int K) {}
int main() {
  int N, K;
  int arr[MAX];

  cin >> N;
  for (int i = 0; i < N; i++) {
    cin >> arr[i];
  }
  cin >> K;

  cout << floor << end;
  cout << ceiling << endl;

  return 0;
}