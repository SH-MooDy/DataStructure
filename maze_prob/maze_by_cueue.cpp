#include <iostream>
#include <queue>
using namespace std;

int main() {
  read_maze();
  queue < pair<int, int> que;
  pair<int, int> cur(0, 0);
  maze[cur.first][cur.second] = -1;
  que.push(cur);

  while (!que.empty()) {
    cur = que.
  }
  return 0;
}